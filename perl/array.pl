use strict;
use warnings;

# arrays

my @number_array = (1..1000);
print @number_array;

my @mix_array = (1, 'mix', 3.14159);
print @mix_array;

my @string_array = qw(Fank Jazz Soul R&B);
print @string_array;
